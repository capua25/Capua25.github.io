---
import { listComments } from "../database/listComments";
import { useTranslation } from "../i18n/utils";
import { labels } from "../i18n/ui";

const currentLang = Astro.currentLocale || "en";
const translated = useTranslation(currentLang as keyof typeof labels);

const comments = await listComments();
let comentarios = [];
if(comments.success){
    comentarios = comments.data;
}
---

<section id="comentarios">
    <div class="max-w-7xl mx-auto">
        <h2 class="text-3xl font-bold text-center text-white mb-8">
            {translated('comments.title')}
        </h2>
        <div class="flex flex-row justify-center flex-wrap gap-6">
            {comentarios.map((comentario) => {
                let date = new Date(comentario.created_at);
                return (
                    <div class="block min-w-96 w-96 p-6 border rounded-lg shadow-sm bg-gray-800 border-gray-700 hover:bg-gray-700">
                        <h3 class="text-xl font-bold text-white mb-2">
                            {comentario.name}
                        </h3>
                        <p class="text-xs text-gray-400 mb-3">
                            {date.toLocaleDateString()}
                        </p>
                        <p class="text-gray-300">
                            {comentario.text}
                        </p>
                    </div>
                );
            })}
        </div>
    </div>
</section>